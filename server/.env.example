# Copy this file to .env and adjust as needed.
# Leave MONGODB_URI empty to auto-run a self-contained MongoDB (mongodb-memory-server).
MONGODB_URI=
PORT=5000
JWT_SECRET=change_me
JWT_EXPIRES=30d
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:5173
